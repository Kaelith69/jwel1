rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Products: public read-only from client
    match /products/{id} {
      allow read: if true;
      allow write: if false;
    }

    // Orders: Loosen rules to allow CREATE from unauthenticated clients
    // while keeping read/update/delete restricted to authenticated users.
    match /orders/{orderId} {
      allow create: if isValidOrder();
      allow read, update, delete: if request.auth != null;
    }

    function isValidOrder() {
      return request.resource.data.orderId is string
        && request.resource.data.status is string
        && request.resource.data.total is number
        && request.resource.data.itemCount is number
        && request.resource.data.items is list
        && request.resource.data.items.size() > 0
        && request.resource.data.customer is map
        && request.resource.data.customer.name is string
        && request.resource.data.customer.mobile is string;
    }

    // Default deny for everything else
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
